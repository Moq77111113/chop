# Go
/chop
*.test
*.out
coverage.txt

# Vite / Node
/web/node_modules
/web/dist
/web/.vite

# Embedded SPA — keep dir + .gitkeep so go:embed compiles, ignore copied build
/internal/dashboard/dist/*
!/internal/dashboard/dist/.gitkeep

# Dev artifacts
/testdata/*.h264
session.ndjson
.DS_Store
