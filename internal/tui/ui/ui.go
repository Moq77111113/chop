// Package ui collects the small framing primitives every component
// composes: keyboard chip, rounded card, state pill, slider track,
// sparkline, section header. Each primitive is a pure function — no
// state, no allocations the caller can't see, no dependency on
// state/data/components.
package ui
